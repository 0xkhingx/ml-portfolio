---
title: your title here
date: 2026-01-01
description: "One-line summary shown on the writing index."
# draft: true   <- uncomment while working; hides this post from the site
---

Frontmatter tips: wrap any value containing a colon (`:`) or leading special
characters in double quotes. Reading time is computed automatically (~200 wpm);
add `minutes: N` to override.

Start writing here.

Regular markdown works: **bold**, *italic*, [links](https://example.com),
`inline code`, lists, quotes, headings, and fenced code blocks with
syntax highlighting:

​```ts
const greeting = "hello";
​```
